This repository keeps its native C++ replay sources under `app/src/main/cpp/`
for historical layout reasons.

The public repository revision does not include a supported Android application,
Android build entrypoint, JNI bridge, or mobile replay workflow.

For the supported public demo, use the host-side scripts documented in the root
`README.md`.
